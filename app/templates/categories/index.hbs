<h2>Všechny kontakty</h2>
<div class="ui divided list">
  {{#each contact in model}}
  <div class="item">
    <i class="map marker icon"></i>
    <div class="content">
      <a class="header">{{contact.name}}</a>
      <div class="description">{{contact.address}}</div>
    </div>
  </div>
  {{/each}}
</div>
